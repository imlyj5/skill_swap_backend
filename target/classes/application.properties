spring.application.name=skill-swap-backend
# Database configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/skillswap_db}
# Server configuration
server.port=${PORT:8080}
